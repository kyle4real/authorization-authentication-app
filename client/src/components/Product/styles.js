import styled from "styled-components";

import { v } from "../../styles/variables";

export const SProductName = styled.span`
    display: block;
    font-weight: 500;
    margin-bottom: ${v.smSpacing};
    text-align: center;
`;

export const SProductPrice = styled.span`
    display: block;
    text-align: center;
`;
