import React from "react";
import { SHome, STitle } from "./styles";

const Home = () => {
    return (
        <SHome>
            <STitle>Welcome Home</STitle>
        </SHome>
    );
};

export default Home;
